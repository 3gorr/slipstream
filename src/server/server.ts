// Server entry. Imports @dcl/sdk/server, so this file must only ever be reached
// through a dynamic import() inside the isServer() branch of src/index.ts.
// Day 1 skeleton — no game loop yet.

export function startServer() {
  console.log('[SERVER] setup')
}
