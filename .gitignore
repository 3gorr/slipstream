# Dependencies
node_modules/

# Decentraland SDK build output
bin/
dist/
.dclcache/
*.crdt

# Secrets — never commit (CLAUDE.md §8)
.env
.env.*

# OS / editor
.DS_Store

# Claude Code local (machine-specific) settings — shared config lives in settings.json / .mcp.json
.claude/settings.local.json
